BoxLayout:
    orientation: 'vertical'
    Button:
        text: 'Stiskni mě'
        background_color: 1, 1, 1, 1
        on_press: self.background_color = app.klik()
            #self.background_color = [random.random() for i in range(3)] + [1]